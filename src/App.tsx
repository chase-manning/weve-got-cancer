import styled from "styled-components";

const StyledApp = styled.div`
  width: 100%;
  min-height: 100dvh;
  background: var(--bg);
`;

const App = () => {
  return <StyledApp>meow</StyledApp>;
};

export default App;
